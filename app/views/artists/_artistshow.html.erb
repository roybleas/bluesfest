<h2><%= artist.name%> 
		<%= render partial: "shared/bluelink", locals: {artist_code: artist.linkid}  %>
</h2>
<table class="table table-striped">
	<tbody>
		<%= render partial: "artists/artistperformances", locals: { performances: performances , startdate_minus_one: startdate_minus_one } %>	
	</tbody>
</table>
